# RVC6D release dependencies (Python >= 3.10, Linux + NVIDIA GPU).
# One command installs everything, including the CUDA-12.8 PyTorch build
# that the released weights were validated with:
#   pip install -r requirements.txt
--extra-index-url https://download.pytorch.org/whl/cu128
torch==2.8.0+cu128
torchvision==0.23.0+cu128
numpy>=1.24
opencv-python
pillow
open3d
pandas
matplotlib
tensorboard
